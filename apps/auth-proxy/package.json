{
  "name": "@rooots/auth-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nitro build",
    "clean": "rm -rf .turbo node_modules",
    "upgrade": "pnpm upgrade --latest",
    "dev": "nitro dev --port 3001",
    "lint": "eslint .",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@auth/core": "^0.18.0"
  },
  "devDependencies": {
    "@rooots/tailwind-config": "workspace:^0.1.0",
    "@rooots/tsconfig": "workspace:^0.1.0",
    "nitropack": "^2.8.1",
    "typescript": "^5.3.3"
  }
}